server:
  port: 8082

spring:
  application:
    name: product-service

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://34.67.255.55:3306/eca_product_db?createDatabaseIfNotExist=true
    username: root
    password: Hasindu123@#

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/