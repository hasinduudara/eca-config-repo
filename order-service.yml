server:
  port: 8083

spring:
  application:
    name: order-service
  
  cloud:
    gcp:
      project-id: hasinduudaraportfolio
      firestore:
        database-id: eca-order-db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/