server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eca_user_db
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/